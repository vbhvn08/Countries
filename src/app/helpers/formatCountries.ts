// import { Country } from "../types"

// export const formatCountries = (countries: any) => {
//   return countries.map((country: any) => {
//     return {
//       cca3: country.cca3,
//       name: country.name.common,
//       population: country.population,
//       flag: country.flag,
//       languages: Object.values(country.languages).map((language: any) => language).join(", "),
//       // flag: `<img src="${country.flag}" alt="${country.name.common}" width="30px" />`,
//       currencies: Object.values(country.currencies).map((currency: any) => currency.name).join(", "),
//     } as Country
//   })

// }